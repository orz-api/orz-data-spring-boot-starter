CREATE TABLE `secondary_test_1`
(
    `id` BIGINT       NOT NULL AUTO_INCREMENT,
    `f1` VARCHAR(100) NOT NULL,
    PRIMARY KEY (`id`)
);

CREATE TABLE `test`
(
    `id` VARCHAR(100) NOT NULL,
    `f1` VARCHAR(100) NOT NULL,
    PRIMARY KEY (`id`)
);
